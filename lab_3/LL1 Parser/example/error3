def : int main(){
    int id=0;
	
        call : id=yylex();
    }while(id != 0);
    call : printf("SYMBOL TABLE ");
    call : showTable();
    
    for(int i = 0; i<100; i=i+1)
    {
		if (i=1)
		{
			call : printf("Iam one");
			i = 5;
		}
		else 
		{
			call : printf("Iam not one");
			if(i=7)
			{
			continue;
			};
		};
    };
};

def : int showTable( )
{
	a = b *c;
	while (a)
	{
		if (a < b < c)
		{
			break;
		};
		
		a = b / c + 2 * 8;
	};
};$
